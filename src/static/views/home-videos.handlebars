 {{!-- offset column --}}
<div class="col-md-1"></div>
{{#each model.videos as |video|}}
<div class="col-xs-12 col-md-2 ">
    <img src="{{video.src}}" alt="{{video.title}} {{video.date}}" class="nh-img-fit">
</div>
{{/each}}
